name: QA — Regressão Visual

on:
  pull_request:
    paths:
      - 'app-mobile/**'

jobs:
  visual-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Instalar dependências de E2E
        run: npm install @playwright/test

      - name: Instalar browsers do Playwright
        run: npx playwright install chromium --with-deps

      - name: Executar Testes de Screenshot (Simulado)
        run: |
          echo "Executando comparação visual do novo design..."
          # Aqui correria o script de screenshot comparativo
          # npx playwright test --grep @visual
          echo "Comparação visual concluída com sucesso."
